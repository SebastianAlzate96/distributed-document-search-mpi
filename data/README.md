# Data

The dataset is generated with this command:

    python3 scripts/generate_documents.py 100000

The generated file documents.csv is not included in the repository because it is large.

In the experiments, the dataset used had 100,000 documents.
